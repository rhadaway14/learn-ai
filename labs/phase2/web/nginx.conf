server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://phase2-api:8080;
    proxy_set_header Host $host;
  }

  location = /health {
    proxy_pass http://phase2-api:8080/health;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
