"use strict";
const api=`http://${location.hostname}:8081`;
const status=document.querySelector("#apiStatus"),history=document.querySelector("#history"),formStatus=document.querySelector("#formStatus");
const escapeHtml=value=>String(value).replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]));
async function load(){try{const health=await fetch(`${api}/health`);if(!health.ok)throw new Error("health check failed");status.textContent="Connected";status.className="status-ok";const data=await fetch(`${api}/api/experiments`).then(response=>response.json());history.innerHTML=data.experiments.length?data.experiments.map(item=>`<article><strong>${escapeHtml(item.activity_id)}</strong><p><b>Hypothesis:</b> ${escapeHtml(item.hypothesis)}</p><p><b>Observed:</b> ${escapeHtml(item.observation)}</p><p><b>Conclusion:</b> ${escapeHtml(item.conclusion)}</p><small>${escapeHtml(item.created_at)}</small></article>`).join(""):"<p>No evidence saved yet.</p>";}catch(error){status.textContent="Unavailable";status.className="status-error";history.innerHTML="<p>Start the foundations profile and reload this page.</p>";}}
document.querySelector("#experimentForm").addEventListener("submit",async event=>{event.preventDefault();const lesson=Number(document.querySelector("#lesson").value),payload={lesson,activity_id:`L${String(lesson).padStart(2,"0")}-A1`,hypothesis:document.querySelector("#hypothesis").value,observation:document.querySelector("#observation").value,conclusion:document.querySelector("#conclusion").value};const response=await fetch(`${api}/api/experiments`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)}),body=await response.json();formStatus.textContent=response.ok?"Evidence saved.":body.error;formStatus.className=response.ok?"status-ok":"status-error";if(response.ok)load();});load();
