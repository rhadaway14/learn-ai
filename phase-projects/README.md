# Phase-Project Artifacts

These records implement the contracts in `schemas/phase-project-increment.schema.json`. Lessons 1–3 provide the reference sequence under `phase1/`; later lessons should follow the same evidence-to-capability flow.
